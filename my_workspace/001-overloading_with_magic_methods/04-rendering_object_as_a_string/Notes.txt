Rather than explicitly telling an object to display itself, it's more convenient to act upon the object in a standardized way, without having to figure out what the method to call is or how it works.

In computer science, this concept is known as polymorphism. In practice, this means there is a common functional interface across multiple classes.
